DATABASE_URL="postgresql://postgres:postgres@localhost:5432/postgres"

# Authentication (NextAuth.js)
# You can generate a secret with `openssl rand -base64 32`
AUTH_SECRET="seu-segredo-aqui"

# Google GenAI (Genkit)
# Get an API key at https://aistudio.google.com/
GOOGLE_GENAI_API_KEY="sua-chave-api-google-genai-aqui"

# Email Configuration (Nodemailer)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="user@example.com"
SMTP_PASSWORD="password"
SMTP_FROM='"AEP Contact" <noreply@aepbr.org.br>'
CONTACT_EMAIL="cultura@aepbr.org.br"