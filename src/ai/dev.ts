
import { genkit } from "genkit";
import { ai } from "./genkit";

// This file is used as the entry point for the Genkit dev server
// defined in package.json scripts: "genkit:dev": "genkit start -- tsx src/ai/dev.ts"

export default ai;
